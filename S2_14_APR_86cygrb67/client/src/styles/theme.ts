// theme.ts
import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: { main: '#4a90e2' },
    secondary: { main: '#50c9c3' },
  },
});

export default theme;
